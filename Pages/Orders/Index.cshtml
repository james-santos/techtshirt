@page
@model techtshirt.Pages.Orders.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Order[0].status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order[0].total_sale_price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order[0].total_pieces)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order[0].reference_code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order[0].date_placed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order[0].date_shipped)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.Order[0].customer_id)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Order) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.total_sale_price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.total_pieces)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.reference_code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date_placed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date_shipped)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.customer_id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
