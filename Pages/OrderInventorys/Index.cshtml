@page
@model techtshirt.Pages.OrderInventorys.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Order_Inventory[0].Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order_Inventory[0].Inventory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order_Inventory[0].order_qty)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order_Inventory[0].total_sale_price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Order_Inventory) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Order.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Inventory.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.order_qty)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.total_sale_price)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
